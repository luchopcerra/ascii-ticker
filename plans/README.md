# Implementation Plans

Generated by the improve skill on 2026-06-29. Initial plans 001-005 were
written against commit `aa2be99`; plans 006-008 were added after an interactive
selection against commit `d20e0ad`. Execute in the order below unless
dependencies say otherwise. Each executor: read the full plan, honor its STOP
conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish a Worker test harness and verification baseline | P1 | M | — | DONE |
| 002 | Require signed X webhook POSTs | P1 | S | 001 | TODO |
| 003 | Escape browser HTML content before linkification | P1 | S | 001 | TODO |
| 004 | Make X bot quote SerpAPI-backed assets correctly | P1 | S | 001 | TODO |
| 005 | Reconcile repo automation with Cloudflare PR previews and main production deploys | P1 | M | — | DONE |
| 006 | Cache non-7d CoinGecko sparkline lookups by asset and range | P2 | M | 001 | TODO |
| 007 | Prove and, if justified, optimize SerpAPI mixed-watchlist lookups | P2 | M | 001 | TODO |
| 008 | Split Worker route orchestration into route modules | P3 | L | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale — finding fixed independently or approach
abandoned)

## Dependency notes

- 001 comes first because the repo currently has no automated tests. It creates
  the verification harness the later runtime plans should use.
- 002, 003, and 004 all depend on 001 so their executors can add or extend
  meaningful regression tests instead of shipping behavior changes with only
  manual checks.
- 005 is independent. It is now P1 because the intended deployment model has
  been clarified by the maintainer: PRs should get Cloudflare previews and
  `main` should update production.
- 006 depends on 001 because it changes a hot market-data path and needs fetch
  counting tests to prove chart caching works across overlapping watchlists.
- 007 depends on 001 because the first step is to measure current SerpAPI
  fetch-count behavior before deciding whether an implementation is justified.
- 008 depends on 001 because the refactor should land only with route-level
  characterization coverage in place.

## Deferred findings not yet planned

- DX-01 — add lint/format tooling. Useful, but still lower leverage than the
  missing test baseline and the selected runtime/performance work.
- DIRECTION-01 — expose `/x-webhook` in help/discovery output and README.
  Valuable for discoverability, but not urgent compared with test, security,
  and hot-path runtime changes.
- DIRECTION-02 — expand wallet coverage beyond ETH/USDC/USDT/LINK. Product
  upside is real, but it should follow after the current reliability backlog.

## Findings considered and rejected

- `src/worker.ts:425-436` symbol-key collision in the SerpAPI merge path:
  rejected for now because the current asset registry and route surface do not
  make it a meaningful near-term risk.
